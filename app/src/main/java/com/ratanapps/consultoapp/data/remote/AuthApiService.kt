package com.ratanapps.consultoapp.data.remote

import retrofit2.http.GET

interface AuthApiService {
    // Auth related endpoints will be added here
    @GET("placeholder")
    suspend fun placeholder()
}