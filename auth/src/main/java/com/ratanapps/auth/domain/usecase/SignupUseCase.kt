package com.ratanapps.auth.domain.usecase

import com.ratanapps.auth.ui.feature.signup.viewmodel.SignUpUIState
import javax.inject.Inject

class SignupUseCase @Inject constructor() {




    /*fun validateSignupForm(signupUIState: SignUpUIState): SignUpUIState {

        if (signupUIState.username.isEmpty()) {
            return false
        } else {
            signupUIState.copy(emailError = "Username cannot be empty")
        }

        return false
    }*/

}